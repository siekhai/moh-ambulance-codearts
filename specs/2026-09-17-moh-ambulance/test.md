# Test Spec: MoH Ambulance Mini App — Sprint 1 E2E

> **Generated by:** tester-agent  
> **Date:** 2026-09-18  
> **Sprint:** Sprint 1 (Foundation)  
> **Scope:** FE-001 (Design Tokens), FE-002 (Khmer i18n), BE-009 (Google Maps Integration), DO-002 (API Key Mgmt)  
> **Test Type:** E2E integration (Playwright)  
> **Environment:** Windows, Node.js, no GOOGLE_MAPS_API_KEY (graceful-degradation path)

---

## Scope & Acceptance Criteria

Sprint 1 delivers foundation only — no full UI screens yet. E2E coverage:

| Deliverable | E2E Coverage |
|---|---|
| FE-001 Design Tokens | CSS custom properties applied to `:root`; brand color `#C80D13`; 140+ tokens present |
| FE-002 Khmer i18n | i18next initializes; default locale `km`; Khmer strings render; `<html lang="km">` |
| BE-009 Google Maps | All 5 REST endpoints respond; validation errors (400); missing-key graceful degradation; `/nearby` ranked list + warning fallback |
| DO-002 API Key Mgmt | `.env.example` documents key; server starts without key |

**Out of E2E scope (covered by unit tests):** `googleMaps.ts` service internals, `formatETA`/`formatDistance` pure functions (34 vitest tests already pass).

---

## Test Cases

### TC-BE-001: Health Endpoint
- Case ID: TC-BE-001
- Case Name: Health endpoint returns ok status
- Module Under Test: Backend server (`/health`)
- Test Type: Functional
- Priority: High

### 2. Preconditions
1. Backend server started on port 3000 without GOOGLE_MAPS_API_KEY

### 3. Test Procedures
#### Step 1
- Action: GET http://localhost:3000/health
- Expected Result: HTTP 200, JSON body `{ "status": "ok" }`

### 4. Postconditions
1. Server still running

---

### TC-BE-002: Maps Geocode — Missing Address Validation
- Case ID: TC-BE-002
- Case Name: Geocode rejects empty address with 400
- Module Under Test: `/api/maps/geocode`
- Test Type: Boundary
- Priority: High

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/geocode (no address param)
- Expected Result: HTTP 400, JSON `{ "error": "address query param is required" }`

### 4. Postconditions
1. None

---

### TC-BE-003: Maps Geocode — Missing API Key Graceful Degradation
- Case ID: TC-BE-003
- Case Name: Geocode returns 400 MISSING_KEY when no API key configured
- Module Under Test: `/api/maps/geocode`
- Test Type: Exception
- Priority: High

### 2. Preconditions
1. Backend server running WITHOUT GOOGLE_MAPS_API_KEY

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/geocode?address=Phnom%20Penh
- Expected Result: HTTP 400, JSON contains `status: "MISSING_KEY"` and error message

### 4. Postconditions
1. None

---

### TC-BE-004: Maps Reverse Geocode — Invalid Coordinates Validation
- Case ID: TC-BE-004
- Case Name: Reverse geocode rejects non-numeric coords with 400
- Module Under Test: `/api/maps/reverse-geocode`
- Test Type: Boundary
- Priority: High

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/reverse-geocode?lat=abc&lng=xyz
- Expected Result: HTTP 400, JSON `{ "error": "lat and lng must be numeric" }`

### 4. Postconditions
1. None

---

### TC-BE-005: Maps Reverse Geocode — Missing API Key
- Case ID: TC-BE-005
- Case Name: Reverse geocode returns 400 MISSING_KEY without key
- Module Under Test: `/api/maps/reverse-geocode`
- Test Type: Exception
- Priority: High

### 2. Preconditions
1. Backend server running WITHOUT key

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/reverse-geocode?lat=11.5564&lng=104.9282
- Expected Result: HTTP 400, JSON contains `status: "MISSING_KEY"`

### 4. Postconditions
1. None

---

### TC-BE-006: Maps Directions — Missing Route Params Validation
- Case ID: TC-BE-006
- Case Name: Directions rejects missing numeric params with 400
- Module Under Test: `/api/maps/directions`
- Test Type: Boundary
- Priority: High

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/directions (no params)
- Expected Result: HTTP 400, JSON error mentions numeric params required

### 4. Postconditions
1. None

---

### TC-BE-007: Maps Directions — Missing API Key
- Case ID: TC-BE-007
- Case Name: Directions returns 400 MISSING_KEY without key
- Module Under Test: `/api/maps/directions`
- Test Type: Exception
- Priority: High

### 2. Preconditions
1. Backend server running WITHOUT key

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/directions?originLat=11.55&originLng=104.92&destLat=11.56&destLng=104.93
- Expected Result: HTTP 400, JSON contains `status: "MISSING_KEY"`

### 4. Postconditions
1. None

---

### TC-BE-008: Maps Distance Matrix — Missing Params Validation
- Case ID: TC-BE-008
- Case Name: Distance matrix rejects missing origins/destinations with 400
- Module Under Test: `/api/maps/distance-matrix`
- Test Type: Boundary
- Priority: High

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/distance-matrix (no params)
- Expected Result: HTTP 400, JSON error mentions origins and destinations required

### 4. Postconditions
1. None

---

### TC-BE-009: Maps Distance Matrix — Missing API Key
- Case ID: TC-BE-009
- Case Name: Distance matrix returns 400 MISSING_KEY without key
- Module Under Test: `/api/maps/distance-matrix`
- Test Type: Exception
- Priority: High

### 2. Preconditions
1. Backend server running WITHOUT key

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/distance-matrix?origins=11.55,104.92&destinations=11.56,104.93
- Expected Result: HTTP 400, JSON contains `status: "MISSING_KEY"`

### 4. Postconditions
1. None

---

### TC-BE-010: Maps ETA — Invalid Coords Validation
- Case ID: TC-BE-010
- Case Name: ETA rejects non-numeric coords with 400
- Module Under Test: `/api/maps/eta`
- Test Type: Boundary
- Priority: High

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/eta?originLat=foo&originLng=bar&destLat=1&destLng=2
- Expected Result: HTTP 400, JSON error mentions numeric params

### 4. Postconditions
1. None

---

### TC-BE-011: Maps ETA — Missing API Key
- Case ID: TC-BE-011
- Case Name: ETA returns 400 MISSING_KEY without key
- Module Under Test: `/api/maps/eta`
- Test Type: Exception
- Priority: High

### 2. Preconditions
1. Backend server running WITHOUT key

### 3. Test Procedures
#### Step 1
- Action: GET /api/maps/eta?originLat=11.55&originLng=104.92&destLat=11.56&destLng=104.93
- Expected Result: HTTP 400, JSON contains `status: "MISSING_KEY"`

### 4. Postconditions
1. None

---

### TC-BE-012: Ambulances Nearby — Invalid Lat/Lng Validation
- Case ID: TC-BE-012
- Case Name: Nearby rejects non-numeric lat/lng with 400
- Module Under Test: `/api/ambulances/nearby`
- Test Type: Boundary
- Priority: High

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/ambulances/nearby?lat=abc&lng=xyz
- Expected Result: HTTP 400, JSON `{ "error": "lat and lng must be numeric" }`

### 4. Postconditions
1. None

---

### TC-BE-013: Ambulances Nearby — Invalid Radius Validation
- Case ID: TC-BE-013
- Case Name: Nearby rejects non-positive radius with 400
- Module Under Test: `/api/ambulances/nearby`
- Test Type: Boundary
- Priority: High

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/ambulances/nearby?lat=11.55&lng=104.92&radius=-5
- Expected Result: HTTP 400, JSON error mentions radius must be positive

### 4. Postconditions
1. None

---

### TC-BE-014: Ambulances Nearby — Graceful Degradation Without Key
- Case ID: TC-BE-014
- Case Name: Nearby returns available ambulances with warning when no key
- Module Under Test: `/api/ambulances/nearby`
- Test Type: Exception
- Priority: High

### 2. Preconditions
1. Backend server running WITHOUT GOOGLE_MAPS_API_KEY

### 3. Test Procedures
#### Step 1
- Action: GET /api/ambulances/nearby?lat=1.3521&lng=103.8198&radius=20000
- Expected Result: HTTP 200; JSON has `ambulances` array (only `available` status units); each item has `distanceMeters: null`, `etaSeconds: null`; response has `warning` field mentioning GOOGLE_MAPS_API_KEY

### 4. Postconditions
1. None

---

### TC-BE-015: Ambulances Nearby — Response Shape Correctness
- Case ID: TC-BE-015
- Case Name: Nearby response shape matches contract
- Module Under Test: `/api/ambulances/nearby`
- Test Type: Functional
- Priority: High

### 2. Preconditions
1. Backend server running WITHOUT key

### 3. Test Procedures
#### Step 1
- Action: GET /api/ambulances/nearby?lat=1.3521&lng=103.8198&radius=50000
- Expected Result: HTTP 200; body has `ambulances`, `origin {lat,lng}`, `radius` (number); each ambulance has `id` (string), `status` ("available"), `location {lat,lng}`

### 4. Postconditions
1. None

---

### TC-BE-016: Ambulances Nearby — Filters Out Busy/Offline Units
- Case ID: TC-BE-016
- Case Name: Nearby only returns available ambulances
- Module Under Test: `/api/ambulances/nearby`
- Test Type: Functional
- Priority: High

### 2. Preconditions
1. Backend server running (fleet has AMB-001 available, AMB-002 available, AMB-003 busy)

### 3. Test Procedures
#### Step 1
- Action: GET /api/ambulances/nearby?lat=1.3521&lng=103.8198&radius=50000
- Expected Result: `ambulances` contains only `status: "available"` units (AMB-001, AMB-002); AMB-003 (busy) excluded

### 4. Postconditions
1. None

---

### TC-BE-017: Ambulance Request Stub
- Case ID: TC-BE-017
- Case Name: POST /api/ambulances/request returns stub order
- Module Under Test: `/api/ambulances/request`
- Test Type: Functional
- Priority: Medium

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: POST /api/ambulances/request with JSON body `{}`
- Expected Result: HTTP 200, JSON has `orderCode` and `ambulanceId` keys

### 4. Postconditions
1. None

---

### TC-BE-018: Order Status Stub
- Case ID: TC-BE-018
- Case Name: GET /api/orders/:id/status returns stub status
- Module Under Test: `/api/orders/:id/status`
- Test Type: Functional
- Priority: Medium

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/orders/abc-123/status
- Expected Result: HTTP 200, JSON has `status` (string) and `timeline` (array)

### 4. Postconditions
1. None

---

### TC-BE-019: Unknown Route Returns 404
- Case ID: TC-BE-019
- Case Name: Unknown API route returns 404
- Module Under Test: Backend routing
- Test Type: Exception
- Priority: Low

### 2. Preconditions
1. Backend server running

### 3. Test Procedures
#### Step 1
- Action: GET /api/nonexistent
- Expected Result: HTTP 404

### 4. Postconditions
1. None

---

### TC-FE-001: Frontend App Loads
- Case ID: TC-FE-001
- Case Name: Vite dev server serves app and root renders
- Module Under Test: Frontend (`/`)
- Test Type: Functional
- Priority: High

### 2. Preconditions
1. Vite dev server running on port 5173

### 3. Test Procedures
#### Step 1
- Action: Navigate to http://localhost:5173/
- Expected Result: Page loads; `<div id="root">` populated; no uncaught console errors blocking render

### 4. Postconditions
1. None

---

### TC-FE-002: Khmer Is Default Locale
- Case ID: TC-FE-002
- Case Name: i18n initializes with Khmer (km) as default
- Module Under Test: i18n config (`frontend/src/i18n/index.ts`)
- Test Type: Functional
- Priority: High

### 2. Preconditions
1. Dev server running

### 3. Test Procedures
#### Step 1
- Action: Read `document.documentElement.lang`
- Expected Result: Value is `"km"`

#### Step 2
- Action: Read rendered app title text
- Expected Result: Text is Khmer `រថយន្តសង្គ្រោះ` (app.title from km.json), not English "Ambulance"

### 4. Postconditions
1. None

---

### TC-FE-003: Design Token CSS Variables Applied
- Case ID: TC-FE-003
- Case Name: Brand color and key design tokens present on :root
- Module Under Test: Design tokens (`frontend/src/styles/tokens.css`)
- Test Type: Functional
- Priority: High

### 2. Preconditions
1. Dev server running

### 3. Test Procedures
#### Step 1
- Action: Evaluate `getComputedStyle(document.documentElement).getPropertyValue('--color-brand')`
- Expected Result: Value is `#C80D13` (brand red, per NFR-4)

#### Step 2
- Action: Count CSS custom properties on `:root` via stylesheet scan
- Expected Result: 140+ custom properties defined (FE-001 contract)

### 4. Postconditions
1. None

---

### TC-FE-004: Fonts Loaded
- Case ID: TC-FE-004
- Case Name: Kantumruy Pro, Inter, Roboto font families declared
- Module Under Test: Font loading (`frontend/src/styles/fonts.css`)
- Test Type: Functional
- Priority: High

### 2. Preconditions
1. Dev server running

### 3. Test Procedures
#### Step 1
- Action: Inspect document stylesheets for `@font-face` rules
- Expected Result: Font families `Kantumruy Pro`, `Inter`, `Roboto` all declared

#### Step 2
- Action: Check `document.fonts.check("1em 'Kantumruy Pro'")` or font load status
- Expected Result: Kantumruy Pro font family registered (CDN @import may still be loading; family declaration present)

### 4. Postconditions
1. None

---

### TC-FE-005: Khmer Subtitle Renders
- Case ID: TC-FE-005
- Case Name: App subtitle renders Khmer ministry text
- Module Under Test: i18n + App.tsx
- Test Type: Functional
- Priority: Medium

### 2. Preconditions
1. Dev server running

### 3. Test Procedures
#### Step 1
- Action: Read rendered subtitle paragraph text
- Expected Result: Text is Khmer `ក្រសួងសុខាភិបាល` (app.subtitle from km.json)

### 4. Postconditions
1. None

---

### TC-VIS-001: Visual Diff — Sprint 1 Scope Check
- Case ID: TC-VIS-001
- Case Name: Visual diff against Figma (limited — Sprint 1 has no full UI)
- Module Under Test: Frontend vs figma-extract.md
- Test Type: Scenario Test
- Priority: Low

### 2. Preconditions
1. Dev server running
2. Figma assets in `specs/2026-09-17-moh-ambulance/assets/`

### 3. Test Procedures
#### Step 1
- Action: Capture screenshot of current app state; compare against Figma `main-screen.png`
- Expected Result: Sprint 1 renders only title+subtitle (no full UI). Full-screen pixel diff NOT expected to match — documented as deferred to Sprint 2+. Capture baseline screenshot for future regression.

### 4. Postconditions
1. Baseline screenshot saved to `test-report/screenshots/`

---

## Coverage Summary

| Suite | Cases | Priority |
|---|---|---|
| Backend API E2E (TC-BE-*) | 19 | High/Med/Low |
| Frontend Smoke E2E (TC-FE-*) | 5 | High/Med |
| Visual Diff (TC-VIS-*) | 1 | Low |
| **Total** | **25** | |

**Coverage rate** computed from passing cases / total cases at execution time.